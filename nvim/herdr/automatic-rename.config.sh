# herdr-automatic-rename config - see config.example.sh in the plugin dir for all options.
# Installed by nvim/install.sh to ~/.config/herdr-automatic-rename/config.sh,
# only when absent.

# No "[N] " jump-key prefix on tab names. Naming the kind (rather than
# AUTO_INDEX=0) also strips prefixes already on existing tabs.
AUTO_INDEX_TABS=0

# Same for workspace names (strips "[1] " already on existing workspaces).
AUTO_INDEX_WORKSPACES=0
